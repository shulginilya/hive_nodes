import styles from './cluster_menu.module.scss';

const ClustersMenuComponent: React.FC = () => {
	return (
		<div className={styles.cluster_menu}>
			cluster menu placeholder
		</div>
	)
};

export default ClustersMenuComponent;
